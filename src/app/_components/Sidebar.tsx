import { AppShellNavbar } from "@mantine/core";

const Sidebar: React.FC = () => {
  return (
    <AppShellNavbar bg="blue" c="white" h="full">
      Digichat
    </AppShellNavbar>
  );
};

export default Sidebar;
